# This is a sort of fake version of LSUV init (Mishkin and Matas, 2016). Instead
# of sequentially scaling each layer to unit-variance, we just scale this layer
# assuming a given variance for the previous layer.

